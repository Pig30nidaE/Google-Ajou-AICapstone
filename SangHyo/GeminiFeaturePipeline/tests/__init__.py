"""Zero-fit, zero-API contract tests.

Nothing in this package fits a model, calls Gemini, or reads more than a few
rows of the real data.  Run them locally with::

    python -m pytest SangHyo/GeminiFeaturePipeline/tests -q
"""
