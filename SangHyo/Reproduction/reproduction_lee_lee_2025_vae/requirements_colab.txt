# Colab 전용 고정 버전. Colab이 이미 깔아둔 torch/CUDA를 건드리지 않기 위해
# torch는 여기 포함하지 않는다 — run.py의 _ensure_dependencies()가 torch 유무만
# 확인하고, 없을 때만 (로컬 환경 한정) 설치를 시도한다.
numpy>=1.26,<3
pandas>=2.1,<3
scipy>=1.11,<2
scikit-learn>=1.4,<2
pyyaml>=6.0,<7
xgboost>=2.0,<4
pytorch-tabnet>=4.1,<5
imbalanced-learn>=0.11,<1
pyarrow>=14.0
tabulate>=0.9
