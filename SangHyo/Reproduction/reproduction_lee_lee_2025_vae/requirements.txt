# --- 필수 (--inspect-data / --dry-run / pytest 는 이것만으로 동작) ---
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
pyyaml>=6.0
scipy>=1.10          # 합성자료 진단의 Wasserstein 거리

# --- 학습에 필요 (지연 임포트) ---
torch>=2.0           # VAE / DNN / Wide&Deep
xgboost>=2.0
pytorch-tabnet>=4.1
imbalanced-learn>=0.11   # SMOTE baseline

# --- 선택 ---
matplotlib>=3.7      # PCA/UMAP 투영 그림
pyarrow>=14.0        # provenance parquet (없으면 CSV로 대체)
tabulate>=0.9        # 결과표 markdown 출력
umap-learn>=0.5      # UMAP 투영 (미설치 시 자동 생략)

# --- 개발 ---
pytest>=7.4
